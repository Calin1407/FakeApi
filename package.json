{
  "name": "fake-api",
  "description": "This is a Fake Api to TerraTech Project",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port 3000 --routes routes.json db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}